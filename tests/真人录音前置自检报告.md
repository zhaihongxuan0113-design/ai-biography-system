# 真人录音前置自检报告

> 检查日期：2026-09-06 ｜ 检查人：项目执行助手（Codex）
> 目标：真人录音测试启动前，确认环境、参数、输出规则全部就绪，确保「录音上传 → 转写 → AI 整理 → 带原声二维码 PDF」一次跑通。

## 一、环境自检结果（全部 ✅）

| # | 检查项 | 结果 | 说明 |
|---|--------|------|------|
| 1 | `skills/audio-to-text.md` | ✅ | 文件完整，含 CrisperWhisper 2.0 调用方式与真人语音参数（v4） |
| 2 | `skills/text-to-story.md` | ✅ | 文件完整，含指南第二步规则 + 真人场景补充规则 |
| 3 | `skills/story-to-book.md` | ✅ | 文件完整，支持真/模拟两组分别成书 |
| 4 | DeepSeek API 连通性 | ✅ | 调用 `deepseek-chat` 问答成功（HTTP 200，返回「正常」，本次消耗 11 tokens） |
| 5 | `tests/fixtures/`（录音存放） | ✅ | 已存在（现有 2 段模拟录音） |
| 6 | `tests/transcripts/`（转写输出） | ✅ | 已存在（现有 2 份模拟转写稿） |
| 7 | `tests/stories/`（故事输出） | ✅ | 已存在（现有 2 份模拟故事稿） |
| 8 | `auto-process.yml` 工作流配置 | ✅ | 触发条件（push fixtures / 手动）、每步自动重试 2 次、失败通知均正常 |

## 二、参数优化完成项

### 1. 转写参数（audio-to-text，适配中文老年语音）
- ✅ 强制指定语言：中文 `language="zh"`（脚本内固定）；
- ✅ 自动标点、断句：由 Whisper 模型自带能力输出，脚本不做二次加工；
- ✅ 慢语速/停顿多优化：停顿标注阈值 `PAUSE_SEC` 由 1.2 秒收紧到 **0.8 秒**，更贴近老人呼吸节奏；
- ✅ 语气保留：`mode="verbatim"` 逐字转写，口头禅、语气词、重复、说一半的话全部保留；
- ✅ 停顿标「……」、笑声标「[笑]」、叹气标「[叹气]」；
- ✅ 不修正语法、不润色、不删除跑题内容（与《语气保留指南》第一步一致）。

### 2. AI 整理参数（text-to-story，适配真人口语）
- ✅ 已读取 `docs/语气保留指南.md` 第二步原文，继续作为系统提示词基础规则；
- ✅ 新增 5 条真人场景补充规则（与指南冲突时以补充规则为准）：
  1. 允许内容跑题，保留真实聊天感，跑题内容不删除；
  2. 保留口头禅、俗语、地方话，不解释、不替换、不翻译，必要时小括号注明；
  3. 只梳理逻辑顺序、拆分段落，绝不改写措辞、不补充虚构细节；
  4. 每段加一个小标题，概括回忆主题；
  5. 结尾提炼一句老人原话作为金句，一字不改。

### 3. 输出规则确认（真/模拟互不覆盖）
- ✅ 真人测试所有输出统一加「真人测试_」前缀：
  - 转写：`tests/transcripts/真人测试_第1周_问题1_转写.md`
  - 故事：`tests/stories/真人测试_第1周_问题1_故事.md`
  - 成书：`tests/books/真人测试_第1辑.pdf`
- ✅ 模拟测试文件继续使用原命名（`测试长者001_*`），两组互不覆盖；
- ✅ 已用 12 项命名解析逻辑测试验证（全部 PASS），3 个脚本 `py_compile` 语法检查通过。

## 三、真人录音命名约定（给测试者）

- 规范式：`真人测试_第1周_问题1_20260906.m4a`（前缀 + 周次 + 问题号 + 日期）
- 简式：`真人测试_录音01_童年_小时候的家.wav`（前缀 + 录音序号，自动换算周次问题号）
- 上传方式：把录音放进 `tests/fixtures/` 并推送（push），流水线自动触发；也可在 Actions 页手动 Run workflow。

## 四、最终结论

**就绪，可开始录音 ✅**

环境自检全部通过；转写参数已按中文老年语音优化；AI 整理提示词已补充真人场景规则；输出命名规则已确认，真人测试文件与模拟测试文件互不覆盖。

备注：DeepSeek 余额有限（上一轮查得约 ¥261），真人测试每次整理会消耗少量 tokens，建议录音上传前再查一次余额。
